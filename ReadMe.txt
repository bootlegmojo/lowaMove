
To connect to the website use the url: http://35.189.123.249

------Localmysql-------------
In order to run the server localy:
Install the MySQL server with username = "host"
                              password = "host"
                              host = "127.0.0.1"
                              port = 3306
The database can be imported via the dump file

run the server.r file
------Localcsv-----------------
Hash out the database commands within function.R
Unhash the readcsv command
ensure the iowadatafile is present within the same directory as the server function and ui.R files
